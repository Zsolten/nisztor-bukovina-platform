# Use a browser-restricted key that only has the Maps JavaScript API enabled.
VITE_GOOGLE_MAPS_API_KEY=

# Optional but recommended: a JavaScript map ID linked to the published cloud map style.
VITE_GOOGLE_MAPS_MAP_ID=
