/**
 * Admin authentication module.
 *
 * <p>Owns owner/platform-admin login, JWT issuing and security-facing user roles.
 * Guest users intentionally do not belong here because public booking requests are anonymous.</p>
 */
package edu.bbte.guesthouse_platform.auth;
